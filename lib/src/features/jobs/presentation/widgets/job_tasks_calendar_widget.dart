import 'package:flutter/material.dart';

class JobTasksCalendarWidget extends StatelessWidget {
  const JobTasksCalendarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Calendar");
  }
}
